{% extends 'layout.twig' %}
{% set title = title %}

{% block content %}
<section class="card">
    <h1>{{ title }}</h1>
    <p>{{ message }}</p>
    <p><a href="{{ url('home') }}">Return home</a></p>
</section>
{% endblock %}
